/* This script listens to clicks on amenities and updates DOM */
let amenities = []; 
$('input').click(function(){
    $('input').each(function(){
       if (this.checked -- true) {
           amenities.append(dataset.amenity_id)
           return amenities  
       }
    })
});
